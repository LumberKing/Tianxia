# Definitions of the dynamically defined modifiers by other elements such as special troop

north_germanic_opinion = { # Not in vanilla, so we need it!
	show_as_percent = no
	is_good = yes
	is_monthly = no
	is_hidden = no
	max_decimals = 0
}

gamer_opinion = { # Not in vanilla, so we need it!
	show_as_percent = no
	is_good = yes
	is_monthly = no
	is_hidden = no
	max_decimals = 0
}

lunatic_opinion = { # Not in vanilla, so we need it!
	show_as_percent = no
	is_good = yes
	is_monthly = no
	is_hidden = no
	max_decimals = 0
}

possessed_opinion = { # Not in vanilla, so we need it!
	show_as_percent = no
	is_good = yes
	is_monthly = no
	is_hidden = no
	max_decimals = 0
}

gluttonous_opinion = { # Not in vanilla, so we need it!
	show_as_percent = no
	is_good = yes
	is_monthly = no
	is_hidden = no
	max_decimals = 0
}

physician_opinion = { # Not in vanilla, so we need it!
	show_as_percent = no
	is_good = yes
	is_monthly = no
	is_hidden = no
	max_decimals = 0
}

dwarf_opinion = { # Not in vanilla, so we need it!
	show_as_percent = no
	is_good = yes
	is_monthly = no
	is_hidden = no
	max_decimals = 0
}

jurchen_opinion = { # Not in vanilla, so we need it!
	show_as_percent = no
	is_good = yes
	is_monthly = no
	is_hidden = no
	max_decimals = 0
}