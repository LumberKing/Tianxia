faction_penglai_independence = { # Special faction to break free from a failure's realm
	type = character

	warning_level = 0.70

	# Plotter scope
	potential = {
		has_character_flag = penglai_failure_revolt
		is_landed = yes
		higher_real_tier_than = BARON
		liege = {
			reverse_has_opinion_modifier = {
				who = PREV
				modifier = opinion_penglai_failure
			}
		}
		is_adult = yes
		independent = no
		prisoner = no
		is_incapable = no # Works fine here
		holy_order = no
		in_seclusion = no
		NOT = {
			trait = in_hiding
		}
		
		NOR = {
			trait = treasure_fleet_preparation
			trait = treasure_fleet_voyage
		}
	}

	# Target scope
	allow = {
		prisoner = no
	}

	# Faction member scope
	# ROOT is joiner
	# FROM is target
	allow_join = {
		ROOT = {
			is_ruler = yes
			has_character_flag = penglai_failure_revolt
			is_adult = yes
			independent = no
			prisoner = no
			is_incapable = no # Works fine here
			holy_order = no
			in_seclusion = no
			NOT = {
				trait = in_hiding
			}
			
			NOR = {
				trait = treasure_fleet_preparation
				trait = treasure_fleet_voyage
			}
		}
	}

	# AI creation weight
	# FROM is faction creator
	# ROOT is title scope
	chance = {
		factor = 1000
	}

	# AI membership weight
	# ROOT is the prospective member
	# FROM is the faction leader
	# FROMFROM is the target title or character
	membership = {
		factor = 1000
	}

	success = {
		independent = yes
	}

	effect = {
		FROM = {
			any_faction_backer = {
				faction = faction_penglai_independence

				reverse_opinion = {
					modifier = opinion_grateful
					who = FROM
					years = 5
				}
				
				clr_character_flag = penglai_failure_revolt
			}
			
			clr_character_flag = penglai_failure_revolt
		}
	}

	abort = {
		always = no	# Factions will abort if the Potential or Allow trigger is no longer valid
	}
}