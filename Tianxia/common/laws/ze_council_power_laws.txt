# These laws give the council more or less power over certain decisions

law_groups = {
	law_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	religious_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	war_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	alliance_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	nap_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	grant_title_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	transfer_vassal_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	grant_vice_royalty_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	settle_adventurer_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	revoke_title_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	retract_vassal_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	plot_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	ransom_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	release_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	imprison_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	banish_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	execution_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	replace_mercenary_captain_voting_power = {
		law_type = council
		allowed_for_councillors = yes
	}
	succession_voting_power = {
		law_type = council
		allowed_for_councillors = no
	}
}

laws = { # TIANXIATODO: Offer vassalization/grant independence
	# COUNCIL POWER LAWS
	##############################
	
	# VOTE ON LAW CHANGES
	law_voting_power_0 = {
		group = law_voting_power
		default = yes

		demesne_size = 2

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_1
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				show_scope_change = no

				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}
				trigger_else = {
					has_law = law_voting_power_1
					trigger_if = {
						limit = {
							OR = {
								has_law = religious_voting_power_0
								has_law = religious_voting_power_1
							}
						}
						has_law = religious_voting_power_0
					}
					has_law = war_voting_power_0
					has_law = alliance_voting_power_0
					has_law = nap_voting_power_0
					has_law = grant_title_voting_power_0
					trigger_if = {
						limit = {
							OR = {
								has_law = transfer_vassal_voting_power_0
								has_law = transfer_vassal_voting_power_1
							}
						}
						has_law = transfer_vassal_voting_power_0
					}
					trigger_if = {
						limit = {
							OR = {
								has_law = grant_vice_royalty_voting_power_0
								has_law = grant_vice_royalty_voting_power_1
							}
						}
						has_law = grant_vice_royalty_voting_power_0
					}
					trigger_if = {
						limit = {
							OR = {
								has_law = settle_adventurer_voting_power_0
								has_law = settle_adventurer_voting_power_1
							}
						}
						has_law = settle_adventurer_voting_power_0
					}
					has_law = revoke_title_voting_power_0
					trigger_if = {
						limit = {
							OR = {
								has_law = retract_vassal_voting_power_0
								has_law = retract_vassal_voting_power_1
							}
						}
						has_law = retract_vassal_voting_power_0
					}
					has_law = plot_voting_power_0
					has_law = ransom_voting_power_0
					has_law = release_voting_power_0
					has_law = imprison_voting_power_0
					has_law = banish_voting_power_0
					has_law = execution_voting_power_0
					trigger_if = {
						limit = {
							OR = {
								has_law = replace_mercenary_captain_voting_power_0
								has_law = replace_mercenary_captain_voting_power_1
							}
						}
						has_law = replace_mercenary_captain_voting_power_0
					}
					has_law = succession_voting_power_0
					

#					trigger_if = {
#						limit = { is_tribal = yes }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						OR = {
							has_law = grand_chancellor_law_1
							has_law = grand_chancellor_law_2
						}
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						OR = {
							has_law = grand_chancellor_law_1
							has_law = grand_chancellor_law_2
						}
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = eunuch_law_2
							has_law = eunuch_law_3
							has_law = eunuch_law_4
						}
					}
					
					OR = {
						has_law = eunuch_law_0
						has_law = eunuch_law_1
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_courtier_law_2
							has_law = ci_courtier_law_3
							has_law = ci_courtier_law_4
						}
					}
					
					OR = {
						has_law = ci_courtier_law_0
						has_law = ci_courtier_law_1
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_2
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
					}
				}
			}
		}

		pass_effect = {
			# log = "law_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = law_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "law_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = laws

			custom_tooltip = {
				text = council_off_custom
				disable_council_voting_on_issue = enforce_peace
			}

			custom_tooltip = { text = tooltip_disables_advisors }
			revoke_law = law_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	law_voting_power_1 = {
		group = law_voting_power

		potential = {
			is_law_potential = law_voting_power_0
		}

		allow = {
			has_law = law_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_1
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "law_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = law_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "law_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = laws

			custom_tooltip = {
				text = council_on_custom
				enable_council_voting_on_issue = enforce_peace
			}

			custom_tooltip = { text = tooltip_enables_advisors }
			revoke_law = law_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 867.1.1
			}

			additive_modifier = {
				value = 10

				OR = {
					title = e_hre
					title = e_byzantium
				}
			}

			additive_modifier = {
				value = 10

				holder_scope = {
					OR = {
						#is_nomadic = yes
						is_tribal = yes
						is_republic = yes
					}
				}
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}
	
	# VOTE ON RELIGIOUS ACTIONS
	religious_voting_power_0 = {
		group = religious_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = religious_voting_power_1

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "religious_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = religious_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "religious_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = ask_for_excommunication_interaction
			disable_council_voting_on_issue = ask_to_lift_excommunication_interaction
			# TIANXIATODO: Softcoded religious decisions, e.g. conversion demands, mass baptism
			revoke_law = religious_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	religious_voting_power_1 = {
		group = religious_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = religious_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = religious_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "religious_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = religious_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "religious_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = ask_for_excommunication_interaction
			enable_council_voting_on_issue = ask_to_lift_excommunication_interaction
			revoke_law = religious_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1204.5.16
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}

	# VOTE ON WAR
	war_voting_power_0 = {
		group = war_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_1
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}
				trigger_else = {
					has_law = law_voting_power_1
					trigger_if = {
						limit = {
							OR = {
								has_law = religious_voting_power_0
								has_law = religious_voting_power_1
							}
						}
						has_law = religious_voting_power_0
					}
					has_law = war_voting_power_1
					has_law = alliance_voting_power_0
					has_law = nap_voting_power_0
					has_law = grant_title_voting_power_0
					trigger_if = {
						limit = {
							OR = {
								has_law = transfer_vassal_voting_power_0
								has_law = transfer_vassal_voting_power_1
							}
						}
						has_law = transfer_vassal_voting_power_0
					}
					trigger_if = {
						limit = {
							OR = {
								has_law = grant_vice_royalty_voting_power_0
								has_law = grant_vice_royalty_voting_power_1
							}
						}
						has_law = grant_vice_royalty_voting_power_0
					}
					trigger_if = {
						limit = {
							OR = {
								has_law = settle_adventurer_voting_power_0
								has_law = settle_adventurer_voting_power_1
							}
						}
						has_law = settle_adventurer_voting_power_0
					}
					has_law = revoke_title_voting_power_0
					trigger_if = {
						limit = {
							OR = {
								has_law = retract_vassal_voting_power_0
								has_law = retract_vassal_voting_power_1
							}
						}
						has_law = retract_vassal_voting_power_0
					}
					has_law = plot_voting_power_0
					has_law = ransom_voting_power_0
					has_law = release_voting_power_0
					has_law = imprison_voting_power_0
					has_law = banish_voting_power_0
					has_law = execution_voting_power_0
					trigger_if = {
						limit = {
							OR = {
								has_law = replace_mercenary_captain_voting_power_0
								has_law = replace_mercenary_captain_voting_power_1
							}
						}
						has_law = replace_mercenary_captain_voting_power_0
					}

#					trigger_if = {
#						limit = { is_tribal = yes }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = eunuch_law_3
							has_law = eunuch_law_4
						}
					}
					
					OR = {
						has_law = eunuch_law_0
						has_law = eunuch_law_1
						has_law = eunuch_law_2
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_courtier_law_3
							has_law = ci_courtier_law_4
						}
					}
					
					OR = {
						has_law = ci_courtier_law_0
						has_law = ci_courtier_law_1
						has_law = ci_courtier_law_2
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "war_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = war_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "war_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = declare_war_interaction
			disable_council_voting_on_issue = ask_to_join_war_interaction
			custom_tooltip = { text = council_war_off_custom }
			revoke_law = war_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	war_voting_power_1 = {
		group = war_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = war_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_1
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "war_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = war_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "war_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = declare_war_interaction
			enable_council_voting_on_issue = ask_to_join_war_interaction
			custom_tooltip = { text = council_war_on_custom }
			revoke_law = war_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 936.8.7
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}
	
	# VOTE ON ALLIANCES
	alliance_voting_power_0 = {
		group = alliance_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = alliance_voting_power_1
					has_law = nap_voting_power_0

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "alliance_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = alliance_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "alliance_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = form_alliance_interaction
			disable_council_voting_on_issue = dissolve_alliance_interaction
			revoke_law = alliance_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	alliance_voting_power_1 = {
		group = alliance_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = alliance_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = alliance_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "alliance_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = alliance_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "alliance_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = form_alliance_interaction
			enable_council_voting_on_issue = dissolve_alliance_interaction
			revoke_law = alliance_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1204.5.16
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}
	
	# VOTE ON ALLIANCES
	nap_voting_power_0 = {
		group = nap_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = nap_voting_power_1

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "nap_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = nap_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "nap_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = form_non_aggression_pact_interaction
			disable_council_voting_on_issue = break_non_aggression_pact
			revoke_law = nap_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	nap_voting_power_1 = {
		group = nap_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = nap_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = alliance_voting_power_1
			has_law = nap_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "nap_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = nap_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "nap_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = form_non_aggression_pact_interaction
			enable_council_voting_on_issue = break_non_aggression_pact
			revoke_law = nap_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1204.5.16
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}

	# VOTE ON GRANT TITLES
	grant_title_voting_power_0 = {
		group = grant_title_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = grant_title_voting_power_1
					trigger_if = {
						limit = {
							OR = {
								has_law = transfer_vassal_voting_power_0
								has_law = transfer_vassal_voting_power_1
							}
						}
						has_law = transfer_vassal_voting_power_0
					}
					trigger_if = {
						limit = {
							OR = {
								has_law = grant_vice_royalty_voting_power_0
								has_law = grant_vice_royalty_voting_power_1
							}
						}
						has_law = grant_vice_royalty_voting_power_0
					}
					trigger_if = {
						limit = {
							OR = {
								has_law = settle_adventurer_voting_power_0
								has_law = settle_adventurer_voting_power_1
							}
						}
						has_law = settle_adventurer_voting_power_0
					}

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						is_any_bureaucrat_trigger = yes
					}
					has_law = alliance_voting_power_0
					has_law = nap_voting_power_0
					has_law = revoke_title_voting_power_0
					trigger_if = {
						limit = {
							OR = {
								has_law = retract_vassal_voting_power_0
								has_law = retract_vassal_voting_power_1
							}
						}
						has_law = retract_vassal_voting_power_0
					}
					has_law = plot_voting_power_0
					has_law = ransom_voting_power_0
					has_law = release_voting_power_0
					has_law = imprison_voting_power_0
					has_law = banish_voting_power_0
					has_law = execution_voting_power_0
					trigger_if = {
						limit = {
							OR = {
								has_law = replace_mercenary_captain_voting_power_0
								has_law = replace_mercenary_captain_voting_power_1
							}
						}
						has_law = replace_mercenary_captain_voting_power_0
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "grant_title_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = grant_title_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "grant_title_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = grant_landed_title_interaction
			revoke_law = grant_title_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	grant_title_voting_power_1 = {
		group = grant_title_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = grant_title_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = grant_title_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "grant_title_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = grant_title_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "grant_title_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = grant_landed_title_interaction
			revoke_law = grant_title_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1204.5.16
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}
	
	# VOTE ON VASSAL_TRANSFER
	transfer_vassal_voting_power_0 = {
		group = transfer_vassal_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = COUNT

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = transfer_vassal_voting_power_1
					trigger_if = {
						limit = {
							OR = {
								has_law = grant_vice_royalty_voting_power_0
								has_law = grant_vice_royalty_voting_power_1
							}
						}
						has_law = grant_vice_royalty_voting_power_0
					}
					trigger_if = {
						limit = {
							OR = {
								has_law = settle_adventurer_voting_power_0
								has_law = settle_adventurer_voting_power_1
							}
						}
						has_law = settle_adventurer_voting_power_0
					}

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						is_any_bureaucrat_trigger = yes
					}
					has_law = alliance_voting_power_0
					has_law = nap_voting_power_0
					has_law = revoke_title_voting_power_0
					trigger_if = {
						limit = {
							OR = {
								has_law = retract_vassal_voting_power_0
								has_law = retract_vassal_voting_power_1
							}
						}
						has_law = retract_vassal_voting_power_0
					}
					has_law = plot_voting_power_0
					has_law = ransom_voting_power_0
					has_law = release_voting_power_0
					has_law = imprison_voting_power_0
					has_law = banish_voting_power_0
					has_law = execution_voting_power_0
					trigger_if = {
						limit = {
							OR = {
								has_law = replace_mercenary_captain_voting_power_0
								has_law = replace_mercenary_captain_voting_power_1
							}
						}
						has_law = replace_mercenary_captain_voting_power_0
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "transfer_vassal_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = transfer_vassal_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "transfer_vassal_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = transfer_vassal_interaction
			revoke_law = transfer_vassal_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	transfer_vassal_voting_power_1 = {
		group = transfer_vassal_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = transfer_vassal_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = grant_title_voting_power_1
			has_law = transfer_vassal_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "transfer_vassal_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = transfer_vassal_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "transfer_vassal_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = transfer_vassal_interaction
			revoke_law = transfer_vassal_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1204.5.16
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}

	# VOTE ON GRANT VICEROYALTIES
	grant_vice_royalty_voting_power_0 = {
		group = grant_vice_royalty_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			has_dlc = "Charlemagne"
			higher_real_tier_than = DUKE
			is_feudal = yes
			NOT = {
				has_law = vice_royalty_0
			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = grant_vice_royalty_voting_power_1
					trigger_if = {
						limit = {
							OR = {
								has_law = settle_adventurer_voting_power_0
								has_law = settle_adventurer_voting_power_1
							}
						}
						has_law = settle_adventurer_voting_power_0
					}
				}
				
				trigger_if = {
					limit = {
						is_any_bureaucrat_trigger = yes
					}
					has_law = alliance_voting_power_0
					has_law = nap_voting_power_0
					has_law = revoke_title_voting_power_0
					trigger_if = {
						limit = {
							OR = {
								has_law = retract_vassal_voting_power_0
								has_law = retract_vassal_voting_power_1
							}
						}
						has_law = retract_vassal_voting_power_0
					}
					has_law = plot_voting_power_0
					has_law = ransom_voting_power_0
					has_law = release_voting_power_0
					has_law = imprison_voting_power_0
					has_law = banish_voting_power_0
					has_law = execution_voting_power_0
					trigger_if = {
						limit = {
							OR = {
								has_law = replace_mercenary_captain_voting_power_0
								has_law = replace_mercenary_captain_voting_power_1
							}
						}
						has_law = replace_mercenary_captain_voting_power_0
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "grant_vice_royalty_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = grant_vice_royalty_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "grant_vice_royalty_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = grant_vice_royalty_interaction
			revoke_law = grant_vice_royalty_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	grant_vice_royalty_voting_power_1 = {
		group = grant_vice_royalty_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = grant_vice_royalty_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = grant_title_voting_power_1
			trigger_if = {
				limit = {
					OR = {
						has_law = transfer_vassal_voting_power_0
						has_law = transfer_vassal_voting_power_1
					}
				}
				has_law = transfer_vassal_voting_power_1
			}
			has_law = grant_vice_royalty_voting_power_0
		}

		pass_effect = {
			# log = "grant_vice_royalty_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = grant_vice_royalty_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "grant_title_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = grant_vice_royalty_interaction
			revoke_law = grant_vice_royalty_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1204.5.16
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}

	# VOTE ON SETTLE ADVENTURER
	settle_adventurer_voting_power_0 = {
		group = settle_adventurer_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = COUNT

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = settle_adventurer_voting_power_1

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "settle_adventurer_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = settle_adventurer_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "settle_adventurer_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = settle_adventurer_interaction
			revoke_law = settle_adventurer_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	settle_adventurer_voting_power_1 = {
		group = settle_adventurer_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = settle_adventurer_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = settle_adventurer_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "settle_adventurer_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = settle_adventurer_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "settle_adventurer_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = settle_adventurer_interaction
			revoke_law = settle_adventurer_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1204.5.16
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}

	# VOTE ON REVOKE TITLES
	revoke_title_voting_power_0 = {
		group = revoke_title_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = revoke_title_voting_power_1
					trigger_if = {
						limit = {
							OR = {
								has_law = retract_vassal_voting_power_0
								has_law = retract_vassal_voting_power_1
							}
						}
						has_law = retract_vassal_voting_power_0
					}

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
			
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "revoke_title_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = revoke_title_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "revoke_title_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = revoke_title_interaction
			revoke_law = revoke_title_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	revoke_title_voting_power_1 = {
		group = revoke_title_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = revoke_title_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = revoke_title_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "revoke_title_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = revoke_title_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "revoke_title_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = revoke_title_interaction
			revoke_law = revoke_title_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1099.7.15
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}

	# VOTE ON RETRACT VASSAL
	retract_vassal_voting_power_0 = {
		group = retract_vassal_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = COUNT

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = retract_vassal_voting_power_1

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "retract_vassal_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = retract_vassal_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "retract_vassal_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = retract_vassal_interaction
			revoke_law = retract_vassal_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	retract_vassal_voting_power_1 = {
		group = retract_vassal_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = retract_vassal_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = revoke_title_voting_power_1
			has_law = retract_vassal_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "retract_vassal_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = retract_vassal_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "retract_vassal_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = retract_vassal_interaction
			revoke_law = retract_vassal_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1099.7.15
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}
	
	# VOTE ON PLOTS
	plot_voting_power_0 = {
		group = plot_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = plot_voting_power_1

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "plot_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = plot_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "plot_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = abandon_ambition_interaction
			disable_council_voting_on_issue = stop_backing_ambition_interaction
			revoke_law = plot_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	plot_voting_power_1 = {
		group = plot_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = plot_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = plot_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_2
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "plot_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = plot_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "plot_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = abandon_ambition_interaction
			enable_council_voting_on_issue = stop_backing_ambition_interaction
			revoke_law = plot_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1204.5.16
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}

	# VOTE ON RELEASE PRISONERS
	release_voting_power_0 = {
		group = release_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = release_voting_power_1

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "release_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = release_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "imprison_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = release_from_prison_interaction
			revoke_law = release_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	release_voting_power_1 = {
		group = release_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = release_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = release_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "release_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = release_voting_power_1
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "release_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = release_from_prison_interaction
			revoke_law = release_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10


				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1066.9.15
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}

	# VOTE ON RANSOM
	ransom_voting_power_0 = {
		group = ransom_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = ransom_voting_power_1

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "ransom_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = ransom_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "ransom_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = ransom_character_interaction
			revoke_law = ransom_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	ransom_voting_power_1 = {
		group = ransom_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = ransom_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = ransom_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "ransom_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = ransom_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "ransom_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = ransom_character_interaction
			revoke_law = ransom_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10


				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1066.9.15
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}

	# VOTE ON IMPRISON
	imprison_voting_power_0 = {
		group = imprison_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = imprison_voting_power_1

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "imprison_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = imprison_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "imprison_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = imprison_character_interaction
			revoke_law = imprison_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	imprison_voting_power_1 = {
		group = imprison_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = imprison_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = imprison_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "imprison_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = imprison_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "imprison_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = imprison_character_interaction
			revoke_law = imprison_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10


				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1066.9.15
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}

	# VOTE ON BANISH
	banish_voting_power_0 = {
		group = banish_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = banish_voting_power_1

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
			
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "banish_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = banish_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "banish_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = exile_imprisoned_interaction
			revoke_law = banish_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	banish_voting_power_1 = {
		group = banish_voting_power
		default = yes

		vassal_limit = 1

		potential = {
			is_law_potential = banish_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = banish_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "banish_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = banish_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "banish_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = exile_imprisoned_interaction
			revoke_law = banish_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1337.1.1
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}

	# VOTE ON EXECUTE
	execution_voting_power_0 = {
		group = execution_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = execution_voting_power_1

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "execution_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = execution_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "execution_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = execute_imprisoned_interaction
			revoke_law = execution_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}
	}

	execution_voting_power_1 = {
		group = execution_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = execution_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = execution_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "execution_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = execution_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "execution_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = execute_imprisoned_interaction
			revoke_law = execution_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10

				holder_scope = {
					religion = catholic
					is_feudal = yes
				}

				start_date >= 1204.5.16
			}

			additive_modifier = {
				value = 10
				title = e_hre
			}

			additive_modifier = {
				value = 10
				holder_scope = { has_nomadic_tribal_government_trigger = yes }
			}
			
			additive_modifier = {
				value = 100
				holder_scope = {
					is_any_bureaucrat_trigger = yes
				}
			}
		}
	}
	
	# VOTE ON REPLACE MERC
	replace_mercenary_captain_voting_power_0 = { # "Has merc company"
		group = replace_mercenary_captain_voting_power
		default = yes

		potential = {
			has_dlc = "Conclave"
			higher_real_tier_than = BARON
			
			holder_scope = {
				any_mercenary_band = {
					band_creator = {
						has_landed_title = ROOT
					}
				}
			}

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		allow = {
			holder_scope = {
				trigger_if = {
					limit = { has_nomadic_tribal_government_trigger = yes }
					has_nomadic_tribal_government_trigger = no
				}

				trigger_else = {
					has_law = replace_mercenary_captain_voting_power_1

#					trigger_if = {
#						limit = { holder_scope = { is_tribal = yes } }
#						has_law = tribal_organization_4
#					}
				}
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						has_chinese_government_trigger = yes
						
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
					
					NAND = {
						has_law = grand_chancellor_law_2
						any_vassal = {
							has_minor_title = title_grand_chancellor
						}
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = eunuch_law_4
					}
					
					NOT = {
						has_law = eunuch_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						has_law = ci_courtier_law_4
					}
					
					NOT = {
						has_law = ci_courtier_law_4
					}
				}
				
				trigger_if = {
					limit = {
						has_dlc = "Jade Dragon" # Conclave checked in the potential
						OR = {
							government = chinese_imperial_government
							government = eastern_imperial_government
						}
						
						OR = {
							has_law = ci_vassal_law_3
							has_law = ci_vassal_law_4
						}
					}
					
					OR = {
						has_law = ci_vassal_law_0
						has_law = ci_vassal_law_1
						has_law = ci_vassal_law_2
					}
				}
			}
		}

		pass_effect = {
			# log = "replace_mercenary_captain_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = replace_mercenary_captain_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "imprison_voting_power_0 ([This.GetFullBaseName])"
			disable_council_voting_on_issue = replace_mercenary_captain_interaction
			revoke_law = replace_mercenary_captain_voting_power_1
		}

		ai_will_do = {
			factor = 0.05
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10
				has_law = law_voting_power_0
			}
		}
	}

	replace_mercenary_captain_voting_power_1 = {
		group = replace_mercenary_captain_voting_power
		default = yes

		#vassal_limit = 1 # You shouldn't be expected to have a merc company to get this one...

		potential = {
			is_law_potential = replace_mercenary_captain_voting_power_0
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = replace_mercenary_captain_voting_power_0

#			trigger_if = {
#				limit = { holder_scope = { is_tribal = yes } }
#
#				OR = {
#					has_law = tribal_organization_3
#					has_law = tribal_organization_4
#				}
#			}
		}

		pass_effect = {
			# log = "replace_mercenary_captain_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = replace_mercenary_captain_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "replace_mercenary_captain_voting_power_1 ([This.GetFullBaseName])"
			enable_council_voting_on_issue = replace_mercenary_captain_interaction
			revoke_law = replace_mercenary_captain_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}

		default_selection = {
			factor = 0

			additive_modifier = {
				value = 10
				has_law = law_voting_power_1
			}
		}
	}

	# VOTE ON SUCCESSION
	succession_voting_power_0 = {
		group = succession_voting_power
		default = yes

		potential = {
			higher_real_tier_than = DUKE
			has_dlc = "Conclave"

			holder_scope = {
				is_feudal = yes
				holy_order = no
			}
		}

		allow = {
			has_law = succession_voting_power_1
		}

		pass_effect = {
			# log = "succession_voting_power_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = succession_voting_power_1
					is_primary_holder_title = yes
				}

				decreased_council_power_effect = yes
			}
		}

		effect = {
			# log = "succession_voting_power_0 ([This.GetFullBaseName])"
			custom_tooltip = { text = succession_voting_power_0_effect_tooltip }
			revoke_law = succession_voting_power_1
		}

		ai_will_do = {
			factor = 1
		}
	}

	succession_voting_power_1 = {
		group = succession_voting_power

		vassal_limit = 1

		potential = {
			is_law_potential = succession_voting_power_0
			
			holder_scope = {
				is_any_bureaucrat_trigger = no
			}
		}

		allow = {
			has_law = law_voting_power_1
			has_law = war_voting_power_1
			has_law = succession_voting_power_0
		}

		pass_effect = {
			# log = "succession_voting_power_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					has_law = succession_voting_power_0
					is_primary_holder_title = yes
				}

				increased_council_power_effect = yes
			}
		}

		effect = {
			# log = "succession_voting_power_1 ([This.GetFullBaseName])"
			custom_tooltip = { text = succession_voting_power_1_effect_tooltip }
			revoke_law = succession_voting_power_0
		}

		ai_will_do = {
			factor = 0
		}
	}
}