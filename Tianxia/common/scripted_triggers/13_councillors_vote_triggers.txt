councillors_vote_trigger = { # Controls whether the default councillors count as voters
	#always = yes # For now!
	has_dlc = "Conclave" # Doesn't matter otherwise
	NOT = {
		trait = under_regency
	}
}